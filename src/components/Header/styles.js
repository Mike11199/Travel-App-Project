//this was copy and pasted from the code tutorial as it is teaching React, not CSS

import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    tite: {
        color: 'red'
    }
}))